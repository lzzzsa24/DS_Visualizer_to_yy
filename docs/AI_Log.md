日期：12月5日
用途：辅助搭建项目目录结构、生成 .gitignore 配置。
心得：AI 能够快速提供标准的工程化目录结构，省去了查资料的时间。

日期: 12月6日
用途: 辅助实现栈与队列的核心逻辑类、设计自定义异常处理机制，并生成覆盖边界条件的 Pytest 单元测试代码。
心得: AI 非常擅长编写标准化的算法逻辑和穷举测试用例，显著提升了代码的规范性与测试覆盖率，有效加速了测试驱动开发的进程。

日期： 12月7日
用途： 辅助编写 PyQt6 图形界面，特别是自定义画布类 (DSCanvas) 的 paintEvent 绘制逻辑；协助实现 MVC 架构，将后端 Stack 模型与前端界面进行信号连接与数据同步。 
心得： AI 在处理繁琐的 GUI 坐标计算和绘图 API 调用上表现出色，帮我快速实现了“数据驱动视图”的效果。通过这一过程，我更直观地理解了观察者模式在图形化编程中的应用。

日期： 12月10日 
用途： 辅助学习pyqt6的更多操作。扩展可视化系统功能，辅助编写队列（Queue）的横向绘制逻辑（QueueCanvas）；指导重构 MainWindow 代码，引入 QTabWidget 实现多数据结构切换；修复绘图坐标精度问题。 
心得： AI 在代码架构重构（如利用辅助函数实现逻辑解耦）方面提供了极具价值的建议，避免了代码的臃肿。同时，在解决具体的 API 报错（如 Qt 浮点数坐标限制）时非常精准，帮助我快速完成了从“单一演示”到“多功能系统”的迭代。

日期： 12月12日 
用途： 修复 MVC 重构后的 Controller 属性命名错误（如 input_field 引用问题）；实现动态修改栈/队列容量的功能（含 Model 数据截断与 Canvas 重绘）；利用 QSS (Qt Style Sheet) 美化按钮样式（圆角、悬停交互）及调整 QFont 字体粗细。
心得： 深刻体会到变量命名一致性在 MVC 架构中的重要性，细微的命名差异会导致 Controller 无法正确获取 View 组件。同时，通过 QSS 和 QFont 的应用，我意识到 GUI 开发不仅是功能堆叠，良好的视觉反馈对用户体验至关重要。

日期： 12月17日 
用途： 实现《栈之传说》游戏 UI 的构建与 HUD（平视显示器）开发。具体包括：将背包系统从简单的文本列表升级为基于 QGraphicsScene 的垂直可视化栈槽位（槽位动态生成、彩色文字填充）；利用 Qt 父子控件机制（Parent-Child）实现“画中画”悬浮窗；修复窗口初始化时的布局时序 Bug（白边与位置错乱）。 
心得： 深刻体会到 QGraphicsView 框架中“场景-视图-图元”三层结构的灵活性，特别是将 View 作为另一个 View 的子控件来实现 HUD 是一种很好的“画中画”解决方案。同时，踩坑了 GUI 的生命周期问题，意识到 __init__ 阶段窗口尺寸未定，必须依赖 resizeEvent 等事件循环机制来处理精确的布局定位，这对提升用户体验（如消除初始闪烁）至关重要。